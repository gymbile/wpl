{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wpl.dev/data/goal-categories.schema.json",
  "title": "WPL Recommended Goal Categories",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "categories"],
  "properties": {
    "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
    "categories": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9_-]*$" },
          "label": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    }
  }
}
